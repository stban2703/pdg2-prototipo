class User {
    constructor(id, name, fullname, email, role) {
        this.id = id
        this.name = name
        this.fullname = fullname
        this.email = email
        this.role = role
    }
}