let currentNoteView = "tablero"

function getNotes(uid) {
    if (window.location.href.includes("#notes")) {
        const noteList = await getNotes(uid)
    }
}

export function renderNotesList(list) {

}
